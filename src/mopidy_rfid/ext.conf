[rfid]
enabled = true

# Hardware pins (BCM numbering)
pin_rst = 25
pin_button_led = 13

# LED ring settings
led_enabled = true
led_pin = 12
led_count = 16
led_brightness = 60

# Mappings database path (optional, defaults to data_dir/rfid_mappings.db)
mappings_db_path =

